<mvc:View
	controllerName="ui5training.controller.CreateOrder"
	xmlns="sap.m"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:core="sap.ui.core">

	<NavContainer
		id="idWizardNavContainer">
		<pages>
			<Page
				id="idWizardContentPage"
				title="{i18n>createOrderPageTitle}"
				class="customPageTitle"
				showNavButton="true"
				navButtonPress=".onNavBack">
				<content>
					<Wizard
						id="idCreateOrderWizard"
						class="sapUiResponsivePadding--header sapUiResponsivePadding--content"
						complete="wizardCompletedHandler">
						<WizardStep
							id="idSelectCustomerStep"
							title="Select Customer">
							<VBox>
								<Label text="Customer:" labelFor="idCustomerSelect" />
								<ComboBox
									id="idCustomerSelect"
									items="{/Customers}"
									showSecondaryValues="true"
									change=".onCustomerSelected"
									width="80%">
									<core:ListItem
										key="{CustomerID}"
										text="{= ${CompanyName} + ' (' + ${City} + ', ' + ${Country} + ')' }"
										additionalText="{= 'Contact: ' + ${ContactName} + ' | Phone: ' + ${Phone} }" />
								</ComboBox>
							</VBox>
						</WizardStep>
					</Wizard>
				</content>
			</Page>
		</pages>
	</NavContainer>


	<!-- <Page
		title="{i18n>createOrderPageTitle}"
		class="customPageTitle"
		showNavButton="true"
		navButtonPress=".onNavBack">
	</Page> -->
</mvc:View>